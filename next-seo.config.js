const title = "Hyperfunctor sklep"
const description = "To jest testowy sklep o nazwie Hyperfunctor"

const nextSeoConfig = {
  title,
  description,
  openGraps: {
    title,
    description,
    site_name: title
  },
};

export default nextSeoConfig
